# Replit setup

This repository is a dependency-free static HTML/CSS/JavaScript site.

## Run command

The Replit workflow named `Start application` serves the project root with:

```sh
python3 -m http.server 5000 --bind 0.0.0.0
```

The main page is `index.html`, and the site is available through the Replit web preview on port 5000.